

---

# Nookplot

## Agent Setup — 2026-05-05

### Agent Identity
- **Name:** Mante
- **Agent ID:** `0f6a7e9c-94cf-45b3-b4a8-d2fa2d474817`
- **Base Address:** `0xEA0aaD6DFa33D6EA4aec842C24D2015E3A4B3175`
- **Gateway:** v0.5.32
- **Status:** Active, connected

### SDK Issues
- `nookplot-runtime` v0.5.98 has schema drift with API
- `bounties.list()` fails: status field expects string, API returns int
- `economy.get_balance()` fails: missing `revenue.claimable` and `revenue.totalEarned` fields
- Workaround: Use REST API directly for full access

### REST API Findings
- **Guilds:** 16 total guilds exist (endpoint: `/v1/guilds`)
- **Bounties:** Mix of expired, claimed, and open. Bounty 10 (5000 NOOK haiku) expired 2026-03-06
- **Agent visible to others:** Agent Icarus detected in presence
- **Economy:** 3 transactions on record

### CLI
- `npx @nookplot/cli` available globally
- SDK: `pip install nookplot-runtime` / `npm install @nookplot/cli`

### Website
- https://nookplot.com

## 2026-05-05 17:23 (Session 3 continuation)

### New Bankr Key Received
- Key: `[REDACTED-BANKR-OLD]`
- SDK test: ✅ WORKS! Agent created, +14 LITCOIN earned
- But wallet is `0x6a5fcc...` — NOT the recovered `0xC4Cf...`
- Old key `bk_usr_PKTbKztw_...` was discarded (user said "use this one instead")

### Action Items In Progress
1. **GitHub push** — Skill repo ready at `projects/litcoin/standalone-miner/` with SKILL.md, README, miner
2. **Guild join** — Guild 13 (Deep Research Collective) selected. Blocker: need Nookplot agent private key for meta-tx signing. Spawned subagent to find key via `nookplot register` CLI flow.
3. **Miner** — SDK miner running with new Bankr key. Standalone also available.

### Nookplot Agent Wallet Discovery
- Agent `Mante` address: `0xEA0aaD6DFa33D6EA4aec842C24D2015E3A4B3175`
- This is NOT the local wallet (`0xC4Cf...`) — it's a separate agent identity wallet
- The private key is generated during `nookplot register` CLI flow
- The SDK/CLI stores it somewhere (likely ~/.nookplot/ or similar)
- Subagent spawned to run registration and capture the key

### Skill Marketplace Strategy
- LobeHub: ✅ Registered as `Mante-Litcoin-Miner`
- Positioning: `litcoin-standalone-miner` (no Bankr required, uses local wallet)
- BankrBot already has `litcoin-miner` (official SDK version)
- Need to push to GitHub before submitting to marketplaces

### .env Updated
- `BANKR_API_KEY=[REDACTED-BANKR-OLD]`
- Old keys commented out
